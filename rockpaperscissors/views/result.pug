extend layout

block content
    div.card-container
        div.card Player choice
            img(src=`/images/${playerChoice}.jpg`)

            p VS
            div.card PC choice
                img(src=`/images/${pcChoice}.jpg`)


    h3 Winner: #{winner}

    a(href='/') Play Again